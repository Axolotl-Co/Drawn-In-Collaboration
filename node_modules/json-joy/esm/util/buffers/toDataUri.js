import { toBase64 } from '../base64';
export const toDataUri = (buf, params) => {
    let uri = 'data:application/octet-stream;base64';
    for (const key in params)
        uri += `;${key}=${params[key]}`;
    return uri + ',' + toBase64(buf);
};
