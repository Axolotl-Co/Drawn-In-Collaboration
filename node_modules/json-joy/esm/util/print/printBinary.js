export const printBinary = (tab = '', children) => {
    const [left, right] = children;
    let str = '';
    if (left)
        str += `\n${tab}← ${left(tab + '  ')}`;
    if (right)
        str += `\n${tab}→ ${right(tab + '  ')}`;
    return str;
};
