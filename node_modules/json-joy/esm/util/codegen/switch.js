import { dynamicFunction } from './dynamicFunction';
export const createSwitch = (fn, codegen) => {
    let counter = 0;
    const [proxy, set] = dynamicFunction((...args) => {
        if (counter > 2)
            set(codegen());
        counter++;
        return fn(...args);
    });
    return proxy;
};
