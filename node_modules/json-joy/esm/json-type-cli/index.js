import { defaultCodecs } from './defaultCodecs';
import { Cli } from './Cli';
export const createCli = (options) => {
    const cli = new Cli({
        codecs: defaultCodecs,
        ...options,
    });
    return cli;
};
