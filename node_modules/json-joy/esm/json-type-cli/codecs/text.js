import { stringify } from '../../json-text/stringify';
export class CliCodecText {
    id = 'text';
    description = 'Formatted JSON text';
    encode(value) {
        const str = stringify(value);
        return new TextEncoder().encode(str + '\n');
    }
    decode(bytes) {
        throw new Error('Not implemented');
    }
}
