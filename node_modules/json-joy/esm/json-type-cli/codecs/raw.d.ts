import type { CliCodec } from '../types';
export declare class CliCodecRaw implements CliCodec<'raw'> {
    readonly id = "raw";
    readonly description = "Raw data, useful for strings and binary data";
    encode(value: unknown): Uint8Array;
    decode(bytes: Uint8Array): unknown;
}
