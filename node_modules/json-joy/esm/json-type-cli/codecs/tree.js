import { toTree } from '../../json-text/toTree';
export class CliCodecTree {
    id = 'tree';
    description = 'Formatted JSON tree';
    encode(value) {
        const str = toTree(value);
        return new TextEncoder().encode(str + '\n');
    }
    decode(bytes) {
        throw new Error('Not implemented');
    }
}
