import { AbstractOp } from './AbstractOp';
export class AbstractPredicateOp extends AbstractOp {
    apply(doc) {
        const test = this.test(doc);
        if (!test)
            throw new Error('TEST');
        return { doc };
    }
}
