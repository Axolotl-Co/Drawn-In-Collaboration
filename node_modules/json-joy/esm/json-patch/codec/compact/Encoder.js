import { encode } from './encode';
export class Encoder {
    options;
    constructor(options) {
        this.options = options;
    }
    encode(patch) {
        return encode(patch, this.options);
    }
}
