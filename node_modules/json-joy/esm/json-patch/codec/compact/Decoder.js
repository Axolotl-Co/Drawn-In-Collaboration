import { decode } from './decode';
export class Decoder {
    options;
    constructor(options) {
        this.options = options;
    }
    decode(patch) {
        return decode(patch, this.options);
    }
}
