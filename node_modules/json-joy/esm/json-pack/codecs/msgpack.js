import { MsgPackEncoder } from '../msgpack';
import { MsgPackDecoder } from '../msgpack/MsgPackDecoder';
export class MsgPackJsonValueCodec {
    id = 'msgpack';
    format = 1;
    encoder;
    decoder;
    constructor(writer) {
        this.encoder = new MsgPackEncoder(writer);
        this.decoder = new MsgPackDecoder();
    }
}
