import { CborDecoder } from '../cbor/CborDecoder';
import { CborEncoder } from '../cbor/CborEncoder';
export class CborJsonValueCodec {
    id = 'cbor';
    format = 0;
    encoder;
    decoder;
    constructor(writer) {
        this.encoder = new CborEncoder(writer);
        this.decoder = new CborDecoder();
    }
}
