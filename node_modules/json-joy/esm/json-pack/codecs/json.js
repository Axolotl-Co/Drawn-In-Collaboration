import { JsonEncoder } from '../json/JsonEncoder';
import { JsonDecoder } from '../json/JsonDecoder';
export class JsonJsonValueCodec {
    id = 'json';
    format = 2;
    encoder;
    decoder;
    constructor(writer) {
        this.encoder = new JsonEncoder(writer);
        this.decoder = new JsonDecoder();
    }
}
