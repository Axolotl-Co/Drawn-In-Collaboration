import { encode as encodeCompact } from '../compact/encode';
import { encode as encodeJson } from '../../../json-pack/cbor/shared';
export const encode = (patch) => {
    return encodeJson(encodeCompact(patch));
};
