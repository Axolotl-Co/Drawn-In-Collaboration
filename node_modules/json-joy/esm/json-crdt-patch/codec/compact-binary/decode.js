import { decode as decodeCompact } from '../compact/decode';
import { decode as decodeJson } from '../../../json-pack/cbor/shared';
export const decode = (buf) => {
    return decodeCompact(decodeJson(buf));
};
