import { Value as V } from '../../../json-type-value/Value';
export class RpcValue extends V {
    data;
    type;
    constructor(data, type) {
        super(type, data);
        this.data = data;
        this.type = type;
    }
}
