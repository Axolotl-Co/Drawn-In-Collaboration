import { Observable } from 'rxjs';
export declare class PubsubService {
    private readonly observers;
    listen$(channel: string): Observable<unknown>;
    publish(channel: string, message: unknown): Promise<void>;
    stats(): {
        channels: number;
        observers: number;
    };
}
