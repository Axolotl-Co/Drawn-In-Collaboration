import { type ITimestampStruct } from '../../../json-crdt-patch/clock';
import type { JsonNode } from '../types';
import type { Printable } from '../../../util/print/types';
export declare class ConNode<View = unknown | ITimestampStruct> implements JsonNode<View>, Printable {
    readonly id: ITimestampStruct;
    readonly val: View;
    constructor(id: ITimestampStruct, val: View);
    children(): void;
    child(): undefined;
    container(): JsonNode | undefined;
    view(): View;
    api: undefined | unknown;
    name(): string;
    toString(tab?: string): string;
}
