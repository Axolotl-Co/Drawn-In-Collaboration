import { Decoder as CompactDecoder } from '../compact/Decoder';
import { Model } from '../../../model';
export declare class Decoder {
    protected decoder: CompactDecoder;
    decode(uint8: Uint8Array): Model;
}
