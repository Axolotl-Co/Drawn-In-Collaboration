import { ITimestampStruct, Patch } from '../../json-crdt-patch';
import { AvlMap } from '../../util/trees/avl/AvlMap';
import { Model } from '../model';
import type { Printable } from '../../util/print/types';
export declare class PatchLog implements Printable {
    readonly start: Model;
    static fromModel(model: Model<any>): PatchLog;
    readonly patches: AvlMap<ITimestampStruct, Patch>;
    constructor(start: Model);
    push(patch: Patch): void;
    replayToEnd(): Model;
    replayTo(ts: ITimestampStruct): Model;
    toString(tab?: string): string;
}
