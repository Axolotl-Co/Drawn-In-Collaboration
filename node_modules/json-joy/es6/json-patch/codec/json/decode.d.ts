import { Op, PredicateOp } from '../../op';
import { Operation } from './types';
import type { JsonPatchOptions } from '../../types';
export declare const operationToOp: (op: Operation, options: JsonPatchOptions) => Op;
export declare const operationToPredicateOp: (op: Operation, options: JsonPatchOptions) => PredicateOp;
export declare function decode(patch: readonly Operation[], options: JsonPatchOptions): Op[];
