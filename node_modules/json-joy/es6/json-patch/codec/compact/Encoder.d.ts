import { Op } from '../../op';
import { CompactOp, EncoderOptions } from './types';
export declare class Encoder {
    readonly options?: EncoderOptions | undefined;
    constructor(options?: EncoderOptions | undefined);
    encode(patch: Op[]): CompactOp[];
}
