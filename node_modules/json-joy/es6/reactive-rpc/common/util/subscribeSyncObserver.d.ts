import { Observable } from 'rxjs';
export interface SyncObserver<T> {
    next: (values: T[], completed: boolean) => void;
    error: (error: unknown) => void;
    complete: () => void;
}
export declare function subscribeSyncObserver<T>(observable: Observable<T>, observer: SyncObserver<T>): import("rxjs").Subscription;
