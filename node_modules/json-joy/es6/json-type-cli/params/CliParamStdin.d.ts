import type { Cli } from '../Cli';
import type { CliParam } from '../types';
export declare class CliParamStdin implements CliParam {
    readonly param = "stdin";
    readonly short = "in";
    readonly title = "Read data from STDIN";
    readonly example = "--in or --in/to=/from";
    readonly createInstance: (cli: Cli, pointer: string, rawValue: unknown) => {
        readonly onStdin: () => Promise<void>;
    };
}
