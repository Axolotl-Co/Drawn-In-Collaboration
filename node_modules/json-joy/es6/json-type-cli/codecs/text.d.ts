import type { CliCodec } from '../types';
export declare class CliCodecText implements CliCodec<'text'> {
    readonly id = "text";
    readonly description = "Formatted JSON text";
    encode(value: unknown): Uint8Array;
    decode(bytes: Uint8Array): unknown;
}
