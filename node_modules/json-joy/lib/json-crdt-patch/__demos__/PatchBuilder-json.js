"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
const clock_1 = require("../clock");
console.clear();
const clock = new clock_1.LogicalClock(123, 456);
const builder = new __1.PatchBuilder(clock);
const json = {
    foo: 'bar',
    baz: 123,
    bools: [true, false],
};
builder.root(builder.json(json));
const patch = builder.flush();
console.log(patch.toString());
const buf = patch.toBinary();
console.log(buf);
const patch2 = __1.Patch.fromBinary(buf);
console.log(patch2.toString());
