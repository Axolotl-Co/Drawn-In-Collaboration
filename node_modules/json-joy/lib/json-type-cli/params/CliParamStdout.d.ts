import type { Cli } from '../Cli';
import type { CliParam } from '../types';
export declare class CliParamStdout implements CliParam {
    readonly param = "stdout";
    readonly short = "out";
    readonly title = "Write data to STDOUT";
    readonly example = "--out=/foo";
    readonly createInstance: (cli: Cli, _: string, rawValue: unknown) => {
        readonly onResponse: () => Promise<void>;
    };
}
