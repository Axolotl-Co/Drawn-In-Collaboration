import type { CliCodec } from '../types';
export declare class CliCodecTree implements CliCodec<'tree'> {
    readonly id = "tree";
    readonly description = "Formatted JSON tree";
    encode(value: unknown): Uint8Array;
    decode(bytes: Uint8Array): unknown;
}
