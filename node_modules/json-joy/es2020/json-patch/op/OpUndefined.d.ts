import type { CompactUndefinedOp } from '../codec/compact/types';
import { AbstractPredicateOp } from './AbstractPredicateOp';
import { OperationUndefined } from '../types';
import { Path } from '../../json-pointer';
import { OPCODE } from '../constants';
import { AbstractOp } from './AbstractOp';
import type { IMessagePackEncoder } from '../../json-pack/msgpack';
export declare class OpUndefined extends AbstractPredicateOp<'undefined'> {
    constructor(path: Path);
    op(): "undefined";
    code(): OPCODE;
    test(doc: unknown): boolean;
    toJson(parent?: AbstractOp): OperationUndefined;
    toCompact(parent: undefined | AbstractOp, verbose: boolean): CompactUndefinedOp;
    encode(encoder: IMessagePackEncoder, parent?: AbstractOp): void;
}
