import { Op, PredicateOp } from '../../op';
import { CompactOp } from './types';
import type { JsonPatchOptions } from '../../types';
export declare const compactToOp: (op: CompactOp, options: JsonPatchOptions) => Op;
export declare const compactToPredicateOp: (op: CompactOp, options: JsonPatchOptions) => PredicateOp;
export declare function decode(patch: readonly CompactOp[], options: JsonPatchOptions): Op[];
