import type { JsonValueCodec } from '../json-pack/codecs/types';
import type { ResolveType, Type } from '../json-type';
export declare class Value<T extends Type> {
    type: T;
    data: ResolveType<T>;
    constructor(type: T, data: ResolveType<T>);
    encode(codec: JsonValueCodec): void;
}
