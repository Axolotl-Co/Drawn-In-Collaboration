import { Printable } from '../../util/print/types';
import { ExtensionDefinition } from './types';
export declare class Extensions implements Printable {
    protected readonly ext: Record<number, ExtensionDefinition>;
    register(extension: ExtensionDefinition<any, any, any>): void;
    get(id: number): ExtensionDefinition | undefined;
    size(): number;
    clone(): Extensions;
    toString(tab?: string): string;
}
