export declare class VectorDelayedValue<T extends unknown[]> {
    readonly slots: T;
    constructor(slots: T);
}
export declare const vec: <T extends unknown[]>(...slots: T) => VectorDelayedValue<T>;
