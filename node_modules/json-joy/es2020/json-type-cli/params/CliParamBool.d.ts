import type { Cli } from '../Cli';
import type { CliParam } from '../types';
export declare class CliParamBool implements CliParam {
    readonly param = "bool";
    readonly short = "b";
    readonly title = "Set boolean value";
    readonly example = "--b/foo=true";
    readonly createInstance: (cli: Cli, pointer: string, rawValue: unknown) => {
        readonly onRequest: () => Promise<void>;
    };
}
